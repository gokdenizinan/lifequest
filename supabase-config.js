// Optional: paste your Supabase values here before deploying to Vercel.
// You can also leave these blank and paste them inside the app's Cloud Save screen.
// The publishable/anon key is safe to use in browser apps when Row Level Security is enabled.

window.LIFEQUEST_SUPABASE_URL = "";
window.LIFEQUEST_SUPABASE_PUBLISHABLE_KEY = "";
